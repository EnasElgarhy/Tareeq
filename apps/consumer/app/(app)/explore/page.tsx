import { ExploreScreen } from "@/components/home/ExploreScreen";

/** The Explore tab — the full career/major/subject map from the CORE report. */
export default function ExplorePage() {
  return <ExploreScreen />;
}
