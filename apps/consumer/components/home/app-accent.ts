/**
 * The post-result app uses a single brand-PURPLE accent — not a per-cluster
 * tint — for the hero card, progress ring, active tab, dots, and card
 * accents, for every user regardless of cluster. (The curiosity-map
 * collection tiles still use their own per-cluster colors.)
 */

/** Brand purple — fills, rings, dots, and text. AA ~5.7:1 on warm paper. */
export const APP_ACCENT = "#6D5BA8";

/** Deep purple — the dark end of the hero gradient (white text rides this). */
export const APP_ACCENT_DEEP = "#221248";
