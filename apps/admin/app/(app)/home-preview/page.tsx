import { HomePreview } from "@/components/home/HomePreview";

/**
 * Internal preview route for the Home Overview feed. Visit /home-preview
 * to review the feed with a seeded report (inside the live app shell).
 * Not linked in the live flow.
 */
export default function HomePreviewPage() {
  return <HomePreview />;
}
