import { DidYouKnowPreview } from "@/components/preview/DidYouKnowPreview";

/**
 * Internal preview route for the Did-You-Know checkpoint screen.
 * Visit /dyk-preview and tap the CTA to cycle through every fact.
 * Not linked anywhere in the live flow.
 */
export default function DidYouKnowPreviewPage() {
  return <DidYouKnowPreview />;
}
