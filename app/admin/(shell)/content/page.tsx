import { SectionStub } from "@/components/admin/SectionStub";

export default function ContentPage() {
  return (
    <SectionStub
      title="Content"
      phase="Phase 1"
      description="Manage clusters, content versions, and questions — then build entirely new assessments with their own scoring config, with draft → preview → publish."
    />
  );
}
