import { SectionStub } from "@/components/admin/SectionStub";

export default function AnalyticsPage() {
  return (
    <SectionStub
      title="Analytics"
      phase="Phase 3–4"
      description="Completion funnels, per-question drop-off, cluster / archetype / driver distributions, geography, and Gemini-vs-fallback AI health."
    />
  );
}
