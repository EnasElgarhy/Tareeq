import { SectionStub } from "@/components/admin/SectionStub";

export default function UsersPage() {
  return (
    <SectionStub
      title="Users"
      phase="Phase 4"
      description="Search and manage users and roles, review consent state, and handle data export + right-to-erasure deletes."
    />
  );
}
