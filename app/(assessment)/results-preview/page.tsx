import { ResultsPreview } from "@/components/preview/ResultsPreview";

/**
 * Internal preview route for the results screen. Visit
 * /results-preview to review the report layout with a seeded sample.
 * Not linked anywhere in the live flow.
 */
export default function ResultsPreviewPage() {
  return <ResultsPreview />;
}
